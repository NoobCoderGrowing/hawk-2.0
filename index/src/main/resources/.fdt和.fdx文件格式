block - > 16kb
[docLength<Vint>, docID<Vint>, docContent<byte>]

.fdt
[bloc<byte>]
no need to store block length, as .fdx stores every block start offset

.fdx
[block start id<int>, block start offset<vlong>]

.tim
